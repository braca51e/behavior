# Serving deps (CPU-only, small) — needed to run the policy server + tests.
numpy>=1.24
msgpack>=1.0
websockets>=12
PyYAML>=6.0

# Dev / test
pytest>=7

# --- Training / real-VLA extras (GPU box only; NOT required to serve the MVP) ---
# torch>=2.0        # real pi0.5 "vla" backend + detector training
# pandas>=1.5       # LeRobot v3 parquet readers (src/training)
# pyarrow           # parquet backend
# huggingface_hub   # demo dataset download (src/training/download)
# scipy             # (optional) stats/VO helpers
